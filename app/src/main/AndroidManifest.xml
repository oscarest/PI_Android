<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.studium.xxracso40xx.pi_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ReproductorMusica" />
        <activity android:name=".list_card" />
        <activity android:name=".PagerView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".pruebaMYSQL"></activity> -->
        <activity android:name=".MainActivity" />
        <activity android:name=".Canciones" />
        <activity android:name=".Principal" />
        <activity android:name=".Perfil" />
        <activity android:name=".Registro" />
        <activity android:name=".Contrasena_Seguridad" />
        <activity android:name=".Contrasena_Cambio" />

        <uses-library android:name="org.apache.http.legacy" />
    </application>
</manifest>